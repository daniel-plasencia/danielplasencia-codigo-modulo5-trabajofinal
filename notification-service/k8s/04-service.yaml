apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: notification-service
spec:
  type: NodePort
  selector:
    app: notification-service
  ports:
    - name: http
      port: 80
      targetPort: 8086
      nodePort: 30086
